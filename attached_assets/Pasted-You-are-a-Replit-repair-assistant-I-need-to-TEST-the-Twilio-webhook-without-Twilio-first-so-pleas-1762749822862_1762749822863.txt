You are a Replit repair assistant. I need to TEST the Twilio webhook without
Twilio first, so please add a dev-friendly test route and a simple health
check, then show me how to verify with curl.

Goals
1) Add a GET /health route that returns 200 JSON { ok: true }.
2) Add a POST /api/voice/test that returns a minimal TwiML response (no Twilio
   signature validation needed for this test).
3) Ensure POST /api/voice/incoming still works and uses the same TwiML format.

Changes to make

A) In server/index.ts (or equivalent):
   - Ensure `app.use(express.urlencoded({ extended: true }));` is enabled because
     Twilio posts application/x-www-form-urlencoded by default.
   - Add:
     app.get("/health", (_req, res) => res.status(200).json({ ok: true }));

B) Create (or update) server/routes/voice.ts to include this TEST route:

   import twilio from "twilio";

   export function registerVoice(app) {
     // existing routes ...

     // --- TEST ROUTE (no signature required) ---
     app.post("/api/voice/test", async (req, res) => {
       const vr = new twilio.twiml.VoiceResponse();
       vr.say({ voice: "Polly.Olivia-Neural" }, "This is a test Twi M L response. The voice system is working.");
       vr.redirect({ method: "POST" }, "/api/voice/handle?route=start");
       return res.type("text/xml").send(vr.toString());
     });

     // Make sure the real incoming route exists:
     app.post("/api/voice/incoming", async (req, res) => {
       const vr = new twilio.twiml.VoiceResponse();
       vr.say({ voice: "Polly.Olivia-Neural" }, "Hello and welcome to your clinic. How can I help you today?");
       vr.pause({ length: 1 });
       vr.redirect({ method: "POST" }, "/api/voice/handle?route=start");
       return res.type("text/xml").send(vr.toString());
     });

     // Ensure /api/voice/handle exists as well (simple stub OK for test):
     app.post("/api/voice/handle", async (req, res) => {
       const route = (req.query.route || "start").toString();
       const vr = new twilio.twiml.VoiceResponse();

       if (route === "start") {
         const g = vr.gather({
           input: "speech",
           language: "en-AU",
           timeout: 5,
           speechTimeout: "auto",
           action: "/api/voice/handle?route=book-day",
           method: "POST",
         });
         g.say({ voice: "Polly.Olivia-Neural" }, "System ready. Would you like to book an appointment?");
         return res.type("text/xml").send(vr.toString());
       }

       if (route === "book-day") {
         const g = vr.gather({
           input: "speech",
           language: "en-AU",
           timeout: 5,
           speechTimeout: "auto",
           action: "/api/voice/handle?route=book-part",
           method: "POST",
         });
         g.say({ voice: "Polly.Olivia-Neural" }, "Which day suits you best?");
         return res.type("text/xml").send(vr.toString());
       }

       // default fallback
       vr.say({ voice: "Polly.Olivia-Neural" }, "Sorry, I didn't catch that. Let's start again.");
       vr.redirect({ method: "POST" }, "/api/voice/handle?route=start");
       return res.type("text/xml").send(vr.toString());
     });
   }

   // Ensure registerVoice(app) is called from server/index.ts.

C) If there is any Twilio signature validation middleware (e.g. validateTwilioSignature),
   DO NOT apply it to /api/voice/test — this route is for local testing only.

D) Rebuild and restart:
   - npm install
   - npm run build
   - npm start

E) Print the exact curl commands for me to run (see next section).
